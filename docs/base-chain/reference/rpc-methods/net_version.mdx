---
title: 'net_version'
sidebarTitle: 'net_version'
description: 'Returns the network ID as a decimal string.'
---

Returns the network ID as a decimal string. For Base, this is the same value as the chain ID.

## Parameters

No parameters.

## Returns

<ResponseField name="result" type="string">
  The network ID as a decimal string (not hex). `"8453"` for Base Mainnet, `"84532"` for Base Sepolia.
</ResponseField>

## Example

<CodeGroup>
```json Request
{
  "jsonrpc": "2.0",
  "method": "net_version",
  "params": [],
  "id": 1
}
```

```json Response (Base Mainnet)
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": "8453"
}
```
</CodeGroup>
